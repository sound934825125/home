Vuetify 组件定义

	






export const VAutocomplete = genericComponent()({

	name: 'VAutocomplete',
	props: {},
	emits: {},

	setup(){
		useRender(() => {
			return _createVNode(VTextField, {}, {})
		})
		return forwardRefs({
			isFocuse,
			isPristine,
			menu,
			search,
			filteeredItems,
			select
		}, vTextFieldRef)
	}

})











